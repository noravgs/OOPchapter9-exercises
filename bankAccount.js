class BankAccount {
  constructor(name, balance, credit) {
    this.name = name;
    this.balance = balance;
    this.credit = credit;
    this.sb = 0;
  }
  this credit = += this.balance;
  
  }
   describe(){
    return `owner: ${this.name}, balance: ${this.balance}`;
}
}

const sean = new BankAccount ("Sean", 100, 0);
const brad = new BankAccount ("Brad", 1000, 0);
const georges = new BankAccount ("Georges", 1000, 0);

console.log(sean.describe());
console.log(brad.describe());
console.log(georges.describe());
